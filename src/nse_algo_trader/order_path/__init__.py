"""The order path — one intent becomes one order, it survives a crash, and the broker is believed.

Feature `F02` (`A.99`). Spec: `docs/research/221`; broker facts: `docs/research/222`; the
regulatory constraints the path is built inside: `docs/research/223`.
"""
